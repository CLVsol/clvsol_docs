===================
Anaconda Python IDE
===================

References:

* `Anaconda Python IDE <http://damnwidget.github.io/anaconda/>`_

**Installation:**

    To install anaconda trough Package Control show the Command Palette (Tools -> Command Palette) and write "install package", then write "anaconda" in the input window popup that just appeared (that is the SublimeText 3 Command Palette) and press "enter".

**Disabling the linter:**

    There is people that doesn’t care about linting or they just use another plugin to do it, they can completely deactivate this feature setting the anaconda_linting as false in the anaconda or project configuration (/home/openerp/.config/sublime-text-3/Packages/Anaconda/Anaconda.sublime-settings).

.. toctree::
   :maxdepth: 2
